[package]
name = "x3-common"
version = "0.1.0"
edition = "2021"
license.workspace = true
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"], default-features = false }

[features]
default = ["std"]
std = ["serde/std"]
