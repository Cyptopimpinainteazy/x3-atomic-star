# SwarmCommand

This placeholder directory defines the X3 Tauri console SwarmCommand app.

It should eventually include UI wiring for:
- feature mode
- backend health
- last test status
- latest chain event
- readiness score
- blockers

Any disabled buttons should render a clear reason instead of being dead.
