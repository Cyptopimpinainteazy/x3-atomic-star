---
phase: 04-rust-build-and-launch-gates
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [REQ-101, REQ-102]
files_modified:
  - scripts/x3_audit.sh
  - .github/workflows/x3-audit.yml
  - .github/scripts/run_real_vm_checks.sh
autonomous: true
user_setup: []
must_haves:
  truths:
    - "CI uses a pinned nightly toolchain for WASM/runtime with rustfmt, clippy, rust-src"
    - "Runtime/WASM build gate runs and fails if wasm-opt is missing"
  artifacts:
    - "Updated x3-audit.yml and x3_audit.sh with Phase 4 build + WASM gates"
  key_links:
    - "scripts/x3_audit.sh"
    - ".github/workflows/x3-audit.yml"
    - ".github/scripts/run_real_vm_checks.sh"
---

# Phase 4 Plan 01: Green cargo build/fmt + WASM runtime gate

## Objective
- **What:** Add Phase 4 build and WASM gates with pinned nightly tooling and wasm-opt enforcement.
- **Why:** REQ-102 requires real Rust build + WASM/runtime gates on the intended release path.
- **Output:** CI + local audit script aligned with runtime/WASM build gate, using pinned nightly in CI.

## Context
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rust-build-and-launch-gates/04-CONTEXT.md
@.planning/phases/04-rust-build-and-launch-gates/04-RESEARCH.md

## Tasks

<task type="code">
  <name>Standardize runtime/WASM build gate and wasm-opt enforcement</name>
  <files>scripts/x3_audit.sh, .github/scripts/run_real_vm_checks.sh</files>
  <read_first>
    - scripts/x3_audit.sh
    - .github/scripts/run_real_vm_checks.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>
    - Use the existing runtime/WASM build command from `.github/scripts/run_real_vm_checks.sh` as the canonical gate:
      - `cd runtime && cargo build --release --target wasm32-unknown-unknown --no-default-features`
    - Add a WASM gate section to `scripts/x3_audit.sh` that runs the same command and fails on error.
    - Require `wasm-opt` in CI by adding a preflight check; if missing, fail the audit.
  </action>
  <acceptance_criteria>
    - `scripts/x3_audit.sh` includes a runtime/WASM build command matching `.github/scripts/run_real_vm_checks.sh`.
    - `scripts/x3_audit.sh` explicitly checks for `wasm-opt` and fails if missing in CI.
  </acceptance_criteria>
</task>

<task type="code">
  <name>Pin nightly toolchain for Phase 4 CI gates</name>
  <files>.github/workflows/x3-audit.yml</files>
  <read_first>
    - .github/workflows/x3-audit.yml
    - rust-toolchain.toml
    - X3_SYSTEMS.md
  </read_first>
  <action>
    - Update `.github/workflows/x3-audit.yml` to use `dtolnay/rust-toolchain@master` with:
      - `toolchain: nightly-2024-12-01` (from `docs/OWNER_RUNBOOK.md` and `docs/DEPLOYMENT.md`)
      - `components: rustfmt, clippy, rust-src`
      - `targets: wasm32-unknown-unknown`
    - Ensure CI still runs `bash scripts/x3_audit.sh --ci` after toolchain setup.
    - Add `binaryen` installation (or equivalent) so `wasm-opt` is present in CI.
  </action>
  <acceptance_criteria>
    - `.github/workflows/x3-audit.yml` uses a pinned nightly toolchain and installs rustfmt/clippy/rust-src.
    - CI setup includes wasm32 target and a `wasm-opt` provider (e.g., `apt-get install binaryen`).
  </acceptance_criteria>
</task>

## Verification
- `rg "nightly-" .github/workflows/x3-audit.yml`
- `rg "wasm-opt|binaryen|wasm32-unknown-unknown" .github/workflows/x3-audit.yml scripts/x3_audit.sh`

## Success Criteria
- [ ] Runtime/WASM build gate is enforced in CI and local audit.
- [ ] CI uses a pinned nightly toolchain with required components.
- [ ] wasm-opt is required and enforced in CI.
