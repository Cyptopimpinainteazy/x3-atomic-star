---
phase: 04-rust-build-and-launch-gates
plan: 03
type: execute
wave: 3
depends_on: [04-02]
requirements: [REQ-101, REQ-102]
files_modified:
  - scripts/x3_audit.sh
  - .github/workflows/x3-audit.yml
  - X3_SYSTEMS.md
  - X3_INDEX.md
  - X3_DEPLOYMENT_SOP.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Launch validator checks run in CI and are documented for local use"
    - "Default config paths are required; missing defaults fail the gate"
  artifacts:
    - "Launch validator gate added to CI and audit script"
  key_links:
    - "crates/x3-launch-validator"
    - "scripts/x3_audit.sh"
    - ".github/workflows/x3-audit.yml"
---

# Phase 4 Plan 03: Green x3-launch-validator and offline checks

## Objective
- **What:** Add launch-validator checks to CI and local audit flow, with strict defaults.
- **Why:** REQ-102 requires launch-validator gate success on the intended release path.
- **Output:** CI runs both launch-validator checks; docs include exact local commands and default config requirements.

## Context
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rust-build-and-launch-gates/04-CONTEXT.md
@.planning/phases/04-rust-build-and-launch-gates/04-RESEARCH.md

## Tasks

<task type="code">
  <name>Wire launch-validator checks into CI and audit script</name>
  <files>scripts/x3_audit.sh, .github/workflows/x3-audit.yml</files>
  <read_first>
    - crates/x3-launch-validator
    - scripts/x3_audit.sh
    - .github/workflows/x3-audit.yml
  </read_first>
  <action>
    - Add two launch-validator commands to `scripts/x3_audit.sh`:
      - `cargo run -p x3-launch-validator -- --check pre-launch`
      - `cargo run -p x3-launch-validator -- --check failure-conditions`
    - Identify the default config paths used by `x3-launch-validator` (from crate docs or code) and add a pre-check that fails if those defaults are missing.
    - Ensure `.github/workflows/x3-audit.yml` runs the same two commands in CI.
  </action>
  <acceptance_criteria>
    - `scripts/x3_audit.sh` includes both launch-validator commands.
    - Missing default config paths cause a hard fail in CI.
    - CI workflow runs both launch-validator checks.
  </acceptance_criteria>
</task>

<task type="docs">
  <name>Document launch-validator gate commands and default path requirements</name>
  <files>X3_SYSTEMS.md, X3_INDEX.md, X3_DEPLOYMENT_SOP.md</files>
  <read_first>
    - X3_SYSTEMS.md
    - X3_INDEX.md
    - X3_DEPLOYMENT_SOP.md
  </read_first>
  <action>
    - Add the two launch-validator commands to the Phase 4 gate list in each doc.
    - Explicitly state that default config paths are required and missing defaults fail the gate.
    - Note that CI runs these checks; local usage is documented for manual validation.
  </action>
  <acceptance_criteria>
    - All three docs list both launch-validator commands.
    - Default config requirement is explicitly documented.
  </acceptance_criteria>
</task>

## Verification
- `rg "x3-launch-validator" scripts/x3_audit.sh .github/workflows/x3-audit.yml`
- `rg "launch-validator|pre-launch|failure-conditions" X3_SYSTEMS.md X3_INDEX.md X3_DEPLOYMENT_SOP.md`

## Success Criteria
- [ ] Launch-validator checks run in CI and are documented for local use.
- [ ] Missing default config paths cause CI failure.
