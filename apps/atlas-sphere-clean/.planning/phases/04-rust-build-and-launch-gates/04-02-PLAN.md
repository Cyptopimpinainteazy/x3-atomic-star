---
phase: 04-rust-build-and-launch-gates
plan: 02
type: execute
wave: 2
depends_on: [04-01]
requirements: [REQ-101, REQ-102]
files_modified:
  - scripts/x3_audit.sh
  - .github/workflows/x3-audit.yml
  - X3_SYSTEMS.md
  - X3_INDEX.md
  - X3_DEPLOYMENT_SOP.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "CI runs release-path tests and fails immediately on failures"
    - "Docs explicitly call out Phase 4 test gates and CI strictness"
  artifacts:
    - "Updated audit script + CI with test gates"
  key_links:
    - "scripts/x3_audit.sh"
    - ".github/workflows/x3-audit.yml"
    - "X3_SYSTEMS.md"
---

# Phase 4 Plan 02: Green targeted and workspace test suites

## Objective
- **What:** Add Phase 4 test gates (workspace + critical crates) with strict CI behavior and documented local usage.
- **Why:** REQ-102 requires real test gates on the release path.
- **Output:** CI and audit script run workspace tests + critical crate tests; docs updated.

## Context
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rust-build-and-launch-gates/04-CONTEXT.md
@.planning/phases/04-rust-build-and-launch-gates/04-RESEARCH.md

## Tasks

<task type="code">
  <name>Add workspace + critical crate test gates</name>
  <files>scripts/x3_audit.sh, .github/workflows/x3-audit.yml</files>
  <read_first>
    - scripts/x3_audit.sh
    - .github/scripts/run_real_vm_checks.sh
    - .planning/codebase/TESTING.md
  </read_first>
  <action>
    - Add a test gate section to `scripts/x3_audit.sh` that runs:
      - `cargo test --workspace --release`
      - `cargo test -p runtime --features std --no-fail-fast`
      - `cargo test -p pallet-x3-kernel --features std --no-fail-fast`
      - `cargo test -p evm-integration --features std --no-fail-fast`
      - `cargo test -p svm-integration --features std --no-fail-fast`
      - `cargo test -p x3-integration --features std --no-fail-fast`
    - Ensure CI fails immediately on any of the above (no retry logic).
    - Keep local behavior consistent, with `--strict` documented for warnings.
  </action>
  <acceptance_criteria>
    - `scripts/x3_audit.sh` includes the workspace + critical crate test commands above.
    - CI runs the same test commands and fails on first error.
  </acceptance_criteria>
</task>

<task type="docs">
  <name>Document Phase 4 test gates and CI strictness</name>
  <files>X3_SYSTEMS.md, X3_INDEX.md, X3_DEPLOYMENT_SOP.md</files>
  <read_first>
    - X3_SYSTEMS.md
    - X3_INDEX.md
    - X3_DEPLOYMENT_SOP.md
    - .planning/phases/04-rust-build-and-launch-gates/04-CONTEXT.md
  </read_first>
  <action>
    - Add Phase 4 test gate commands to the documented gate set in `X3_SYSTEMS.md` and `X3_INDEX.md`.
    - Update `X3_DEPLOYMENT_SOP.md` with Phase 4 test requirements and CI strictness:
      - CI uses `-D warnings`
      - Flaky tests fail immediately
      - Local `--strict` is the explicit opt-in for warning failures
  </action>
  <acceptance_criteria>
    - Phase 4 test commands are listed in all three docs.
    - CI strictness and flaky test policy are explicitly stated.
  </acceptance_criteria>
</task>

## Verification
- `rg "cargo test --workspace --release" scripts/x3_audit.sh .github/workflows/x3-audit.yml`
- `rg "Phase 4|test gate|flaky|--strict" X3_SYSTEMS.md X3_INDEX.md X3_DEPLOYMENT_SOP.md`

## Success Criteria
- [ ] Workspace + critical crate tests are enforced in CI and local audit.
- [ ] Docs clearly state Phase 4 test gates and CI strictness.
