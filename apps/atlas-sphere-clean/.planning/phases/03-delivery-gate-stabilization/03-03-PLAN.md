---
phase: 03-delivery-gate-stabilization
plan: 03
type: execute
wave: 2
depends_on: [03-01]
requirements: [REQ-101]
files_modified:
  - .github/workflows/x3-audit.yml
  - X3_SYSTEMS.md
  - X3_INDEX.md
  - X3_DEPLOYMENT_SOP.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "CI workflow matches the Phase 3 minimal gate set"
    - "Contributor docs list the minimal gate command set explicitly"
  artifacts:
    - "Updated x3-audit.yml and updated contributor documentation"
  key_links:
    - ".github/workflows/x3-audit.yml"
    - "X3_SYSTEMS.md"
    - "X3_INDEX.md"
    - "X3_DEPLOYMENT_SOP.md"
---

# Phase 3 Plan 03: Define minimal release gate command set for contributors

## Objective
- **What:** Align CI with Phase 3 minimal gates and document the exact contributor command set.
- **Why:** REQ-101 requires a repeatable, trustworthy gate set that matches CI and docs.
- **Output:** Updated workflow and docs that specify the Phase 3 command set and defer Phase 4+ gates.

## Context
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-delivery-gate-stabilization/03-CONTEXT.md
@.planning/phases/03-delivery-gate-stabilization/03-RESEARCH.md

## Tasks

<task type="code">
  <name>Align .github/workflows/x3-audit.yml with Phase 3 minimal gates</name>
  <files>.github/workflows/x3-audit.yml</files>
  <read_first>
    - .github/workflows/x3-audit.yml
    - scripts/x3_audit.sh
    - rust-toolchain.toml
  </read_first>
  <action>
    - Replace the existing multi-job workflow (build/test/safety/launch) with a single Phase 3 gate job.
    - Use `actions/checkout@v4`.
    - Install Rust toolchain using `dtolnay/rust-toolchain@master` with `toolchain: 1.88.0` and `components: rustfmt`.
    - Install Node using `actions/setup-node@v4` with `node-version: 20` and `cache: npm`.
    - Run:
      - `chmod +x scripts/x3_audit.sh`
      - `bash scripts/x3_audit.sh --ci`
    - Remove the final `gate` job and any references to build/test/launch-validator or WASM checks.
  </action>
  <acceptance_criteria>
    - `.github/workflows/x3-audit.yml` contains a single Phase 3 gate job that runs `bash scripts/x3_audit.sh --ci`.
    - The workflow no longer runs release builds, full tests, clippy, or launch-validator.
    - The workflow installs Rust and Node before running the audit script.
  </acceptance_criteria>
</task>

<task type="docs">
  <name>Document the Phase 3 minimal gate command set for contributors</name>
  <files>X3_SYSTEMS.md, X3_INDEX.md, X3_DEPLOYMENT_SOP.md</files>
  <read_first>
    - X3_SYSTEMS.md
    - X3_INDEX.md
    - X3_DEPLOYMENT_SOP.md
    - .planning/phases/03-delivery-gate-stabilization/03-CONTEXT.md
  </read_first>
  <action>
    - In `X3_SYSTEMS.md`, update the Automated Audit + CI Gate sections to list the Phase 3 minimal commands:
      - `bash scripts/x3_audit.sh`
      - `bash scripts/x3_audit.sh --ci`
      - `cargo check --workspace`
      - `cargo fmt --all -- --check`
      - `npm run build:all-packages --if-present`
    - In `X3_INDEX.md`, update the quick-start / gating sections with the same minimal command list.
    - In `X3_DEPLOYMENT_SOP.md`, update the pre-commit and CI sections to note Phase 3 scope and defer build/test/launch-validator/WASM to Phase 4.
    - Add a short note that toolchain pinning is documented via `rust-toolchain.toml` but Phase 3 does not enforce additional toolchain locks.
  </action>
  <acceptance_criteria>
    - All three docs explicitly list the Phase 3 minimal gate command set.
    - Docs explicitly defer build/test/launch-validator/WASM gates to Phase 4.
    - Docs mention `rust-toolchain.toml` as the documented toolchain reference.
  </acceptance_criteria>
</task>

## Verification
- `rg "Phase 3|minimal gate|build:all-packages|cargo fmt --all|cargo check --workspace" X3_SYSTEMS.md X3_INDEX.md X3_DEPLOYMENT_SOP.md`
- `rg "bash scripts/x3_audit.sh --ci" .github/workflows/x3-audit.yml`

## Success Criteria
- [ ] CI workflow matches Phase 3 minimal gate set only.
- [ ] Contributor docs list the exact minimal command set.
- [ ] Phase 4+ gates are explicitly deferred in docs.
