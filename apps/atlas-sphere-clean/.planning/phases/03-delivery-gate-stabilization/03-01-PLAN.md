---
phase: 03-delivery-gate-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [REQ-101]
files_modified:
  - scripts/x3_audit.sh
autonomous: true
user_setup:
  - Optional: ensure Rust toolchain + npm are installed for local verification
must_haves:
  truths:
    - "`scripts/x3_audit.sh --ci` exits non-zero on WARN or FAIL"
    - "Missing tools produce FAIL in CI and WARN locally"
  artifacts:
    - "scripts/x3_audit.sh includes preflight checks, `cargo fmt --all -- --check`, and `npm run build:all-packages --if-present`"
  key_links:
    - "scripts/x3_audit.sh"
---

# Phase 3 Plan 01: Repair local audit and packaging scripts

## Objective
- **What:** Make `scripts/x3_audit.sh` enforce Phase 3 minimal gates with CI-strict behavior and preflight checks.
- **Why:** REQ-101 requires trustworthy, repeatable local gates that match CI expectations.
- **Output:** Updated `scripts/x3_audit.sh` with preflight, cargo fmt, npm build, and CI warning failures.

## Context
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-delivery-gate-stabilization/03-CONTEXT.md
@.planning/phases/03-delivery-gate-stabilization/03-RESEARCH.md

## Tasks

<task type="code">
  <name>Add CI-strict preflight checks for required tooling</name>
  <files>scripts/x3_audit.sh</files>
  <read_first>
    - scripts/x3_audit.sh
    - .planning/phases/03-delivery-gate-stabilization/03-CONTEXT.md
  </read_first>
  <action>
    - Add a new section header before Section 1: `header "0. PREFLIGHT"`.
    - Implement a `preflight_tool()` helper that checks tools and uses:
      - `fail "Missing required tool: <tool>"` when `CI_MODE=1`
      - `warn "Missing required tool: <tool>"` when `CI_MODE=0`
    - Call preflight for: `cargo`, `rg`, `npm`, and `rustfmt` (via `rustfmt --version` or `cargo fmt --version`).
    - Ensure preflight is warn-only locally and does not stop execution unless in CI.
  </action>
  <acceptance_criteria>
    - `scripts/x3_audit.sh` contains a `0. PREFLIGHT` header section.
    - `scripts/x3_audit.sh` contains explicit preflight checks for `cargo`, `rg`, `npm`, and `rustfmt`.
    - Preflight uses `fail` in CI and `warn` locally.
  </acceptance_criteria>
</task>

<task type="code">
  <name>Make CI mode strict and add minimal gate checks</name>
  <files>scripts/x3_audit.sh</files>
  <read_first>
    - scripts/x3_audit.sh
    - .planning/phases/03-delivery-gate-stabilization/03-CONTEXT.md
    - package.json
  </read_first>
  <action>
    - When parsing args, set `STRICT=1` whenever `--ci` is present (so warnings fail CI).
    - In Build Integrity, add `cargo fmt --all -- --check` as a hard check with `pass`/`fail` output.
    - Add package build gate using `npm run build:all-packages --if-present` with pass/fail output.
    - Keep `cargo check --workspace` as the Rust build gate (no release build, no tests, no WASM).
  </action>
  <acceptance_criteria>
    - `scripts/x3_audit.sh` contains `cargo fmt --all -- --check`.
    - `scripts/x3_audit.sh` contains `npm run build:all-packages --if-present`.
    - `scripts/x3_audit.sh` sets `STRICT=1` in CI mode.
    - `scripts/x3_audit.sh --ci` exits non-zero when `WARN > 0` (STRICT path preserved).
  </acceptance_criteria>
</task>

## Verification
- `rg "PREFLIGHT|cargo fmt --all|build:all-packages|STRICT=1" scripts/x3_audit.sh`
- Run locally if tools are installed:
  - `bash scripts/x3_audit.sh`
  - `bash scripts/x3_audit.sh --ci`

## Success Criteria
- [ ] `scripts/x3_audit.sh` enforces the Phase 3 minimal gates.
- [ ] CI mode fails on warnings and missing tools.
- [ ] Local mode warns but does not hard-fail on missing tools.
