I have the following comments after thorough review of file. Implement the comments by following the instructions verbatim.

---
## Comment 1: All three SDK HTLC adapters simulate transactions without any actual signing or broadcasting.

For each adapter (`evm.ts`, `solana.ts`, `substrate.ts`, `bitcoin.ts`) replace the simulated `sendTransaction` / `sendSolanaTransaction` / `submitExtrinsic` methods with real implementations:
- For `evm.ts`: Use `ethers.js` v6 (`Wallet.signTransaction` + `provider.broadcastTransaction`) or `viem` (`walletClient.sendTransaction`) to sign with the provided `signerKey` and broadcast via `eth_sendRawTransaction`. Remove the pseudo-hash generation.
- For `solana.ts`: Build a real `Transaction` (or `VersionedTransaction`) using `@solana/web3.js`, sign with the decoded keypair, and call `connection.sendTransaction`. Remove the hash derivation fallback.
- For `substrate.ts`: Use `@polkadot/api`'s `tx.sign(keypair).send()` pattern, using the proper `xxhash128` storage key construction instead of the SHA-256 placeholder in `buildStorageKey`. Remove the simulated `txHash` return.
- For `bitcoin.ts`: Implement PSBT construction, signing, and broadcast via the Esplora REST API (`POST /tx`). Fix `isHTLCExpired()` to query the current block height and compare against the stored `timeLock`.
- Add integration tests that run against a local node/regtest to confirm actual on-chain state changes.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/packages/atomic-swap-sdk/src/htlc/evm.ts
- /home/lojak/Desktop/x3-chain-master/packages/atomic-swap-sdk/src/htlc/solana.ts
- /home/lojak/Desktop/x3-chain-master/packages/atomic-swap-sdk/src/htlc/substrate.ts
- /home/lojak/Desktop/x3-chain-master/packages/atomic-swap-sdk/src/htlc/bitcoin.ts
- /home/lojak/Desktop/x3-chain-master/packages/atomic-swap-sdk/src/swap/orchestrator.ts
---
## Comment 2: `pallet-x3-atomic-kernel/src/lib.rs` is an empty file, making the entire claimed orchestration layer missing.

Recover the contents of `pallets/x3-atomic-kernel/src/lib.rs` from version control or the readiness document blueprint:
1. Check `git log -- pallets/x3-atomic-kernel/src/lib.rs` to find the last non-empty commit.
2. Restore that version: `git checkout <commit_hash> -- pallets/x3-atomic-kernel/src/lib.rs`.
3. Verify the restored file contains the pallet macro, `Config` trait, `BundleState` storage map, `DeadlineIndex` storage map, bond reserve/unreserve logic, `on_initialize` hook with O(1) expiry, and strict finality cert validation as described in `ATOMIC_CROSSVM_PRODUCTION_READINESS.md`.
4. Re-run `cargo test -p pallet-x3-atomic-kernel` to confirm all tests pass.
5. If the file was never written, implement the pallet from the blueprint in the readiness doc.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/pallets/x3-atomic-kernel/src/lib.rs
- /home/lojak/Desktop/x3-chain-master/ATOMIC_CROSSVM_PRODUCTION_READINESS.md
- /home/lojak/Desktop/x3-chain-master/pallets/x3-atomic-kernel/Cargo.toml
---
## Comment 3: AtomicSwap dispatch is not truly atomic — EVM success followed by SVM failure leaves inconsistent cross-VM state.

Fix the atomicity guarantee in `dispatch_operation` within `crates/cross-vm-bridge/src/lib.rs`:
1. For the `AtomicSwap` arm, wrap both VM dispatch calls in a rollback-capable pattern: execute Leg 1 tentatively (in a 'prepare' sub-call), then execute Leg 2. If Leg 2 fails, issue a compensating transaction to reverse Leg 1 (e.g., return funds to the EVM escrow address).
2. Alternatively, enforce true 2PC at the VM level: use the `prepare` phase to obtain lock receipts from both VMs before committing either, and only proceed to `commit` when both have confirmed reserved resources. The existing `try_lock_source`/`try_lock_destination` already checks balances — extend these to actually lock (e.g., transfer to an escrow account) so that the commit phase cannot be preempted.
3. Add a test case that injects a SVM failure mid-swap and asserts the EVM balance is restored.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/crates/cross-vm-bridge/src/lib.rs
---
## Comment 4: Bitcoin HTLC `sha256()` function is a fake polynomial hash, making BTC HTLC and merkle proof verification completely broken.

Replace the fake `sha256` and `hash_pair` functions in `crates/x3-bridge/src/bitcoin_htlc.rs`:
1. Replace `fn sha256(data: &[u8]) -> [u8; 32]` with a call to `sp_io::hashing::sha2_256(data)` (already imported via `sp_io`). This is the correct SHA-256 implementation provided by the Substrate runtime.
2. Replace `fn hash_pair(a: &[u8; 32], b: &[u8; 32]) -> [u8; 32]` with the Bitcoin double-SHA-256 merkle pair hash: `sha2_256(&sha2_256(&combined)[..])` where `combined` is the sorted concatenation of `a` and `b` (Bitcoin hashes pairs in sorted order).
3. Update the existing test `test_sha256` and `test_redeem_correct_preimage` to verify against real SHA-256 vectors.
4. Also update `fn generate_contract_id` to use `sp_io::hashing::sha2_256` for deterministic IDs.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/crates/x3-bridge/src/bitcoin_htlc.rs
---
## Comment 5: `x3_submitCrossVmTransaction` — the key external-chain RPC entry-point described in docs — is never registered in the node.

Wire the missing RPC methods into `node/src/rpc.rs` via the `create_full` function:
1. Register `x3_submitCrossVmTransaction`: accept a JSON body with `evm_payload`, `svm_payload`, and `atomic: bool`. Internally, this should build a `CrossVmOperation::AtomicSwap` (or the appropriate variant), submit it to the `pallet-x3-atomic-kernel` extrinsic queue via `client.runtime_api().submit_comit_v2(...)`, and return the resulting transaction hash.
2. Register `x3_getCanonicalBalance` to call `AtlasKernelRuntimeApi::get_canonical_balance`.
3. Wire `walletDex_estimateSwap`, `walletDex_executeSwap` from `crates/x3-rpc/src/wallet_dex_rpc.rs` into the module merge in `create_full`.
4. Register `atomicTrade_*` endpoints by merging the `SwapRPCServer` methods.
5. Add a Postman/curl integration test confirming each method is reachable on a running dev node.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/node/src/rpc.rs
- /home/lojak/Desktop/x3-chain-master/crates/x3-atomic-trade/src/swap_rpc.rs
- /home/lojak/Desktop/x3-chain-master/docs/rpc.md
---
## Comment 6: `BillingPlan`/`BillingAccount` types exist but there is no billing enforcement — API usage is not metered or gated.

Implement billing enforcement at two levels:

**1. API-level metering (blockchain-connector service):**
- Add an API key authentication middleware to `packages/blockchain-connector/src/server/index.ts` that reads `X-Api-Key` from request headers, looks up the associated `BillingAccount`, and increments `usage.requestsThisMonth`.
- Add quota enforcement: if `quotaRemaining.requests <= 0`, return HTTP 429 with `Retry-After`.
- Implement the `/billing/plans` and `/billing/account` REST handlers.
- Store billing state in a database (Postgres or SQLite) instead of in-memory.

**2. On-chain fee collection (Rust runtime):**
- Wire the `FeeCalculator::calculate_swap_fees()` result into the `pallet-atomic-trade-engine` extrinsic: before executing a cross-VM batch, call `T::Currency::transfer(caller, fee_treasury, protocol_fee)` where `protocol_fee` is computed from the fee calculator.
- Add a test asserting that after a successful cross-VM swap, the fee treasury account balance has increased by the expected protocol fee amount.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/packages/blockchain-connector/src/types/index.ts
- /home/lojak/Desktop/x3-chain-master/packages/blockchain-connector/src/server/index.ts
- /home/lojak/Desktop/x3-chain-master/crates/x3-swap-router/src/fee_calculator.rs
- /home/lojak/Desktop/x3-chain-master/crates/x3-rpc/src/wallet_dex_rpc.rs
- /home/lojak/Desktop/x3-chain-master/packages/blockchain-connector/docs/openapi.yaml
---
## Comment 7: Integration test for cross-VM atomicity uses a fully in-process simulation harness with no real chain interaction.

Replace or supplement the simulated integration harness in `integration-tests/cross-vm-atomic-test.rs`:
1. Fix the syntax error in the test function name: rename `test_cross_<arg_key>vm_atomic_rollback` (line 388) to `test_cross_vm_atomic_rollback`.
2. Create a companion integration test file (e.g., `integration-tests/cross-vm-real-chain-test.rs`) that:
   - Starts a `TestExternalities` with the full runtime (including `pallet-atomic-trade-engine` and `pallet-x3-atomic-kernel`) using the mock.rs configuration.
   - Calls `pallet_atomic_trade_engine::Pallet::<Test>::create_trade_batch(...)` and `execute_trade_batch(...)` within `execute_with`.
   - Verifies storage is updated correctly, events are emitted, and balances are changed.
3. Add a node-level e2e test that connects via WebSocket to a running dev node, submits an `x3_submitCrossVmTransaction` call, and verifies the returned transaction hash corresponds to a finalized block event.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/integration-tests/cross-vm-atomic-test.rs
---
## Comment 8: Gas estimation RPC `x3_call` returns a hardcoded mock byte and `gas_price` returns a magic constant.

In `crates/x3-rpc/src/gas_estimation.rs`:
1. Replace the mock return value in `call()` with an actual state call via the runtime API: route through `AtlasKernelRuntimeApi::submit_evm_transaction` in read-only (dry-run) mode, or use `state_call` on the substrate RPC to simulate the execution.
2. Replace the hardcoded `gas_price()` with a query to the runtime's current base fee: call `AtlasKernelRuntimeApi::get_base_fee(at)` or read from the Frontier fee pallet storage.
3. If a real EVM runner is not yet available, at minimum document these as simulation stubs with a versioned placeholder comment and do not expose them on public-facing RPC until real state is wired.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/crates/x3-rpc/src/gas_estimation.rs
- /home/lojak/Desktop/x3-chain-master/node/src/rpc.rs
---
## Comment 9: The testnet release artifacts (signed tarball, WASM, node binary) have not been generated — Phase 08-03 is pending.

Once the critical bugs in comments 1-7 are resolved, complete Phase 08-03 in `X3_RELEASE_READINESS_CHECKLIST.md`:
1. Build the release binary: `cargo build --release --workspace`.
2. Create checksums and GPG-sign the artifacts as described in Part 7 of the checklist.
3. Test on a clean host by extracting the tarball and running `./x3-chain-node --dev` followed by the health check script.
4. Deploy to the testnet using `deployment/manage-testnet.sh` and verify block production and finality advance.
5. Run the live integration suite: `packages/ts-sdk/tests/live.integration.test.ts` against the deployed testnet endpoint.
6. Check the box for `[ ] Release artifacts prepared` and obtain the engineer and release manager sign-offs on the checklist.

### Relevant Files
- /home/lojak/Desktop/x3-chain-master/X3_RELEASE_READINESS_CHECKLIST.md
- /home/lojak/Desktop/x3-chain-master/X3_DEPLOYMENT_SOP.md
- /home/lojak/Desktop/x3-chain-master/deployment/manage-testnet.sh
---